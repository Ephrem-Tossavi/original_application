<%= form_with(model: [post, comment]) do |form| %>
    
    <div class="mb-6">
      <%= form.label :Contenu %>
      <%= form.text_area :content, class: "input h-64" %>
    </div>
    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>